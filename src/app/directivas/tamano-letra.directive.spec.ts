import { TamanoLetraDirective } from './tamano-letra.directive';

describe('TamanoLetraDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new TamanoLetraDirective();
    expect(directive).toBeTruthy();
  });
});
